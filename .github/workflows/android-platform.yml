# YAML Workflow for Building an Android Debug APK for a Flutter Project

name: Build Flutter Android Debug APK

on:
  push:
    branches:
      - master # Or your development branch
  workflow_dispatch: # Allows manual triggering

jobs:
  build_android_apk:
    name: Build Android Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Project Code
        uses: actions/checkout@v4

      - name: Set up Java JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin' # A popular distribution of OpenJDK
          cache: gradle # Cache Gradle dependencies
          
      - name: Manually Install and Link CMake 3.10.2
        run: |
          # 1. Download
          curl -L https://dl.google.com/android/repository/cmake-3.10.2-linux-x86_64.zip -o cmake.zip
          
          # 2. Extract into a specific temporary folder
          mkdir -p cmake_temp
          unzip -q cmake.zip -d cmake_temp
          
          # 3. Find the extracted folder (it might be nested) and move it
          # We use find to locate the 'bin' directory to be safe
          EXTRACTED_PATH=$(find cmake_temp -name "bin" -type d | sed 's/\/bin//')
          
          sudo mkdir -p /usr/local/cmake-3.10.2
          sudo cp -r $EXTRACTED_PATH/* /usr/local/cmake-3.10.2/
          
          # 4. Create the symbolic link
          sudo ln -sf /usr/local/cmake-3.10.2/bin/cmake /usr/local/bin/cmake
          sudo ln -sf /usr/local/cmake-3.10.2/bin/ninja /usr/local/bin/ninja
          
          # 5. Make sure it's executable
          sudo chmod +x /usr/local/bin/cmake
          
          # 6. Verify
          echo "CMake location: $(which cmake)"
          cmake --version

      - name: Set up Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.24.5
          channel: 'stable'
        # This action sets up the Flutter environment.
        
      - name: Get Flutter Dependencies
        run: flutter pub get
        # Fetches all the dependencies listed in pubspec.yaml.

      - name: Build Android Debug APK
        run: flutter build apk --debug --verbose
        # This is the core command to build the debug APK.
        # The --debug flag specifies a debug build.
        # This build will now use JDK 17 for Gradle.

      - name: Upload APK Artifact (Optional)
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: error # Fails the workflow if the APK is not found
        # This step uploads the generated APK as an artifact,
        # making it available for download from the GitHub Actions run.

      - name: Notify Build Status (Example - further setup required for actual notifications)
        if: always() # Ensures this step runs even if previous steps fail
        run: |
          echo "Build status: ${{ job.status }}"
          echo "APK (if successful) is available in artifacts or at build/app/outputs/flutter-apk/app-debug.apk"
